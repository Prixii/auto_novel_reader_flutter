import 'package:flutter/material.dart';

class WebNovel extends StatelessWidget {
  const WebNovel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('施工中ヾ(≧▽≦*)o');
  }
}
