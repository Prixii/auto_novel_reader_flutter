import 'package:flutter/material.dart';

class NovelRank extends StatelessWidget {
  const NovelRank({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('施工中ヾ(≧▽≦*)o');
  }
}
